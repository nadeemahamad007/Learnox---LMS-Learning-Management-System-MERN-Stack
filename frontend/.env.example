# If the backend serves the frontend (same-origin), keep this as /api.
# If deploying the frontend separately, set it to the full backend URL (e.g. https://your-api.onrender.com/api).
VITE_API_BASE_URL=/api
